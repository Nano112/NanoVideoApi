# NanoVideoApi Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Directory Configuration
DOWNLOADS_DIR=/app/downloads

# Security Configuration
# IMPORTANT: Replace with secure, randomly generated API keys for production
API_KEYS=your-secure-api-key-1,your-secure-api-key-2

# CORS Configuration
# For production, specify exact domains instead of '*'
ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Database URL if you plan to add database support
# DATABASE_URL=postgresql://user:password@localhost:5432/nanovideo

# Optional: Redis URL for caching
# REDIS_URL=redis://localhost:6379/0

# Optional: Maximum file size for downloads (in bytes)
# MAX_DOWNLOAD_SIZE=104857600  # 100MB

# Optional: Maximum concurrent downloads
# MAX_CONCURRENT_DOWNLOADS=5
